# Note

This demo requires the MastgTest.swift code to be integrated into the MASTestApp-iOS project and compiled.
The binary should be extracted from the compiled app and placed in this directory as `MASTestApp`.

Once the binary is available, run `./run.sh` to generate the analysis output.
